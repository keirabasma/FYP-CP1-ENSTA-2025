#include <stdio.h>
#include "menu.h"
#include "departments.h"

int main(){
    // srand(time(NULL));
    // department_tree_node* root = load_department_tree_from_file("departments.txt");
    // if (!root) {root = build_department_tree();}
    // update_department_globals(root);


                    display_main_menu();

    // save_department_tree_to_file("departments.txt", root);
    return 0;
}